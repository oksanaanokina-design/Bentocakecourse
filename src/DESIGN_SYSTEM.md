# Дизайн-система онлайн-школы Оксаны Анокиной

## 🎨 Цветовая палитра

### Основные цвета
```css
/* Фоны */
--bg-primary: #1a1410      /* Основной фон - насыщенный кофейный */
--bg-secondary: #2a1f18    /* Вторичный фон - средний кофейный */

/* Акценты */
--accent: #D4B896          /* Основной акцент - золотисто-бежевый */
--accent-light: #E5C9A7    /* Светлый акцент - для hover состояний */
--accent-dark: #C9A882     /* Тёмный акцент - для градиентов */
```

### Использование в коде
```tsx
// Фоны
className="bg-[#1a1410]"           // Основной фон
className="bg-[#2a1f18]"           // Вторичный фон

// Акценты
className="text-[#D4B896]"         // Золотой текст
className="border-[#D4B896]"       // Золотая рамка
className="bg-[#D4B896]"           // Золотой фон

// Градиенты
className="bg-gradient-to-r from-[#D4B896] to-[#C9A882]"
className="hover:from-[#E5C9A7] hover:to-[#D4B896]"
```

---

## 📏 Spacing (Отступы)

```css
gap-2: 0.5rem   (8px)   /* Минимальные отступы между элементами */
gap-3: 0.75rem  (12px)  /* Малые отступы */
gap-4: 1rem     (16px)  /* Стандартные отступы */
gap-6: 1.5rem   (24px)  /* Средние отступы */
gap-8: 2rem     (32px)  /* Большие отступы */
```

### Примеры
```tsx
className="flex gap-2"    // Для иконки + текст
className="flex gap-3"    // Для списков
className="flex gap-4"    // Для карточек в ряд
className="space-y-6"     // Вертикальные отступы между секциями
className="space-y-8"     // Большие вертикальные отступы
```

---

## 🔲 Border Radius (Скругление)

```css
rounded-lg:   0.5rem   (8px)   /* Кнопки навигации */
rounded-xl:   0.75rem  (12px)  /* Модальные окна, кнопки тарифов */
rounded-2xl:  1rem     (16px)  /* Карточки, секции */
rounded-3xl:  1.5rem   (24px)  /* Большие модальные окна */
```

### Где использовать
```tsx
// Навигация и малые элементы
className="rounded-lg"     // Кнопки "Забронировать", "Курсы"

// Интерактивные элементы
className="rounded-xl"     // Кнопки выбора тарифа, иконки

// Карточки и секции
className="rounded-2xl"    // Карточки курсов, отзывов, "Было/Стало"

// Модальные окна
className="rounded-3xl"    // PaymentModal, PrivacyModal
```

---

## 🌟 Shadows (Тени)

### Стандартные тени
```css
shadow-lg           /* Стандартная большая тень */
shadow-xl           /* Экстра большая тень */
shadow-2xl          /* Максимальная тень */
```

### Золотые тени (фирменные)
```css
shadow-[#D4B896]/10   /* Очень лёгкая золотая тень */
shadow-[#D4B896]/20   /* Лёгкая золотая тень */
shadow-[#D4B896]/30   /* Средняя золотая тень */
shadow-[#D4B896]/40   /* Интенсивная золотая тень (hover) */
```

### Примеры использования
```tsx
// Обычное состояние
className="shadow-lg shadow-[#D4B896]/20"

// Hover состояние
className="hover:shadow-xl hover:shadow-[#D4B896]/40"

// Левитация (карточки)
className="hover:-translate-y-2 hover:shadow-2xl hover:shadow-[#D4B896]/30"
```

---

## ⚡ Animations (Анимации)

### Типы переходов
```css
transition-all       /* Все свойства (универсальный) */
transition-colors    /* Только цвет */
transition-transform /* Только transform (position, scale) */
```

### Длительность
```css
duration-200  /* 200ms - Быстрые анимации (кнопки, hover) */
duration-300  /* 300ms - Средние анимации (модальные окна) */
```

### Дофаминовые эффекты
```tsx
// Увеличение при hover
className="hover:scale-[1.05] transition-all duration-200"

// Уменьшение при клике
className="active:scale-[0.98] transition-all duration-200"

// Стрелка вправо →
className="group-hover:translate-x-1 transition-transform"

// Стрелка вниз ↓
className="group-hover:translate-y-0.5 transition-transform"

// Левитация
className="hover:-translate-y-2 transition-all duration-300"
```

---

## 🎯 Готовые паттерны

### Основная кнопка (CTA)
```tsx
<button className="group bg-gradient-to-r from-[#D4B896] to-[#C9A882] text-[#1a1410] px-8 py-4 rounded-xl hover:from-[#E5C9A7] hover:to-[#D4B896] transition-all shadow-lg shadow-[#D4B896]/20 hover:shadow-[#D4B896]/40 hover:scale-[1.05] active:scale-[0.98] inline-flex items-center gap-2">
  Начать обучение
  <span className="group-hover:translate-x-1 transition-transform">→</span>
</button>
```

### Вторичная кнопка
```tsx
<button className="group px-8 py-4 border-2 border-[#D4B896] text-[#D4B896] rounded-xl hover:bg-[#D4B896]/10 transition-all hover:scale-[1.05] active:scale-[0.98] inline-flex items-center gap-2">
  Подробнее
  <span className="group-hover:translate-y-0.5 transition-transform">↓</span>
</button>
```

### Карточка с левитацией
```tsx
<div className="bg-gradient-to-br from-[#2a1f18] to-[#3d2f22] rounded-2xl p-8 border border-[#D4B896]/20 transition-all duration-300 hover:-translate-y-2 hover:shadow-2xl hover:shadow-[#D4B896]/20">
  {/* Контент карточки */}
</div>
```

### Акцентная карточка
```tsx
<div className="bg-gradient-to-br from-[#D4B896]/15 to-[#C9A882]/10 rounded-2xl p-8 border-2 border-[#D4B896]/40 transition-all duration-300 hover:-translate-y-2 hover:shadow-2xl hover:shadow-[#D4B896]/30">
  {/* Контент карточки */}
</div>
```

### Навигационная кнопка
```tsx
<a className="group bg-gradient-to-r from-[#D4B896] to-[#C9A882] text-[#1a1410] px-5 py-2 rounded-lg hover:from-[#E5C9A7] hover:to-[#D4B896] transition-all text-sm font-semibold hover:scale-[1.05] active:scale-[0.95] hover:shadow-lg hover:shadow-[#D4B896]/30 inline-flex items-center gap-2">
  Забронировать
  <span className="group-hover:translate-y-0.5 transition-transform">↓</span>
</a>
```

---

## 📱 Responsive Design

### Breakpoints (Tailwind по умолчанию)
```css
sm:  640px   /* Малые планшеты */
md:  768px   /* Планшеты */
lg:  1024px  /* Малые десктопы */
xl:  1280px  /* Стандартные десктопы */
2xl: 1536px  /* Большие экраны */
```

### Mobile-First подход
```tsx
// Базовый стиль для мобильных
className="text-2xl"

// Адаптация для планшетов и десктопов
className="text-2xl sm:text-3xl lg:text-4xl"

// Сетка
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"
```

---

## ✨ Специальные эффекты

### Glassmorphism
```tsx
className="bg-[#2a1f18]/80 backdrop-blur-xl"
```

### Градиенты фона
```tsx
// Основной градиент
className="bg-gradient-to-b from-[#1a1410] to-[#0f0c0a]"

// Акцентный градиент
className="bg-gradient-to-br from-[#D4B896]/15 to-[#C9A882]/10"
```

### Золотое свечение (hover)
```tsx
className="hover:shadow-2xl hover:shadow-[#D4B896]/40 transition-all duration-300"
```

---

## 🎨 Иконки и эмодзи

### Рекомендуемые эмодзи
- 🎂 Торты, десерты
- ✨ Премиум, новое
- 🎁 Бонусы
- ✓ Галочки в списках
- 📦 Старая версия
- 🟡 Стандартный тариф
- ⭐ Premium тариф
- 📱 Telegram
- 🎬 Reels, видео

### Иконки (lucide-react)
```tsx
import { Award, TrendingUp, Users, Star } from 'lucide-react';
```

---

## 📝 Типографика

### Заголовки
- Не используйте Tailwind классы для размера шрифта (`text-2xl`) без необходимости
- Стандартные `<h1>`, `<h2>`, `<h3>` уже имеют стилизацию в `globals.css`
- Используйте `font-bold` или `font-semibold` для акцентов

### Примеры
```tsx
<h1 className="font-bold">Заголовок</h1>
<h2 className="text-white">Подзаголовок</h2>
<p className="text-[#D4B896]/80 leading-relaxed">Описание</p>
```

---

## 🚀 Чек-лист использования

- [ ] Все фоны используют `#1a1410` или `#2a1f18`
- [ ] Акценты только `#D4B896`, `#E5C9A7`, `#C9A882`
- [ ] Кнопки имеют `hover:scale-[1.05]` и `active:scale-[0.98]`
- [ ] Интерактивные элементы имеют стрелки → или ↓
- [ ] Карточки имеют `rounded-2xl`
- [ ] Модальные окна имеют `rounded-3xl`
- [ ] Тени используют золотой цвет `shadow-[#D4B896]/XX`
- [ ] Анимации имеют `transition-all duration-300` или `duration-200`
- [ ] Mobile-first: сначала мобильные стили, потом `sm:`, `md:`, `lg:`

---

*Дизайн-система версия 1.0 | Январь 2026*
