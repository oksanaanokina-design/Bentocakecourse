# 🚀 Инструкция по развертыванию на Vercel

## ✅ ЧТО СДЕЛАНО:

1. **Создан файл `vercel.json`** - конфигурация для Vercel
2. **Обновлен `vite.config.ts`** - правильные настройки сборки
3. **Создан `index-vite.html`** - правильная точка входа для Vite

---

## 📋 ШАГ 1: ПЕРЕИМЕНОВАТЬ ФАЙЛЫ

**ВАЖНО!** Перед деплоем на Vercel нужно переименовать файлы:

### В корне проекта:

```bash
# Сохраните старый index.html (если нужен)
mv index.html index-old-standalone.html

# Переименуйте Vite-версию в основную
mv index-vite.html index.html
```

**Или вручную:**
- Переименуйте `index.html` → `index-old-standalone.html`
- Переименуйте `index-vite.html` → `index.html`

---

## 🌐 ШАГ 2: РАЗВЕРТЫВАНИЕ НА VERCEL

### Вариант A: Через GitHub (Рекомендуется)

1. **Загрузите проект на GitHub:**
   - Создайте новый репозиторий на github.com
   - Загрузите все файлы проекта

2. **Подключите Vercel к GitHub:**
   - Откройте [vercel.com](https://vercel.com)
   - Нажмите "Import Project"
   - Выберите ваш репозиторий
   - Vercel автоматически определит настройки

3. **Настройки проекта в Vercel:**
   ```
   Framework Preset: Vite
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

4. **Deploy!**
   - Нажмите "Deploy"
   - Подождите 1-2 минуты
   - Готово! 🎉

### Вариант B: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Разверните проект:**
   ```bash
   vercel
   ```

4. **Для продакшн-деплоя:**
   ```bash
   vercel --prod
   ```

---

## 🔧 ШАГ 3: ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

После успешного деплоя проверьте:

✅ Главная страница открывается  
✅ Переход на `/privacy` работает  
✅ Все ссылки Instagram открываются  
✅ Формы работают  
✅ Модальные окна открываются  
✅ Кнопка "Наверх" работает  
✅ Мобильная версия отображается корректно  

---

## 📁 СТРУКТУРА ФАЙЛОВ:

```
/
├── vercel.json              ← Конфигурация Vercel (✅ создан)
├── vite.config.ts           ← Настройки Vite (✅ обновлен)
├── index.html               ← Точка входа (❗переименуйте из index-vite.html)
├── index-vite.html          ← Новый правильный HTML (✅ создан)
├── index-old-standalone.html ← Старый HTML (сохраните на всякий случай)
├── package.json             ← Зависимости
├── src/
│   ├── main.tsx             ← Входная точка React
│   ├── App.tsx              ← Главный компонент
│   └── ...
└── dist/                    ← Будет создана после сборки
```

---

## 🛠 РЕШЕНИЕ ПРОБЛЕМ:

### Ошибка "No Output Directory named dist found"
✅ **РЕШЕНО** - добавлен `vercel.json` с правильным `outputDirectory`

### Ошибка "Cannot find module"
- Проверьте, что переименовали `index-vite.html` → `index.html`
- Убедитесь, что файл содержит: `<script type="module" src="/src/main.tsx"></script>`

### Ошибка при сборке
```bash
# Локально протестируйте сборку:
npm run build
npm run preview
```

### Роутинг не работает (404 на /privacy)
✅ **РЕШЕНО** - в `vercel.json` добавлены правила `rewrites`

---

## 🎯 ВАЖНО:

1. **Обязательно переименуйте `index-vite.html` → `index.html`**
2. Файл `vercel.json` уже настроен правильно
3. После деплоя домен будет вида: `your-project.vercel.app`
4. Можно подключить свой домен в настройках Vercel

---

## 📞 ПОДДЕРЖКА:

Если возникнут проблемы:
- Проверьте логи в Vercel Dashboard
- Убедитесь, что `index.html` содержит ссылку на `/src/main.tsx`
- Проверьте, что все файлы загружены в GitHub

---

## ✅ ФИНАЛЬНЫЙ ЧЕК-ЛИСТ:

- [ ] Переименовал `index-vite.html` → `index.html`
- [ ] Загрузил проект на GitHub
- [ ] Подключил репозиторий к Vercel
- [ ] Настроил параметры: Framework = Vite, Output = dist
- [ ] Нажал Deploy
- [ ] Проверил работу сайта по ссылке
- [ ] Протестировал на мобильном
- [ ] Все ссылки работают

---

🎉 **УСПЕШНОГО ДЕПЛОЯ!** 🚀
