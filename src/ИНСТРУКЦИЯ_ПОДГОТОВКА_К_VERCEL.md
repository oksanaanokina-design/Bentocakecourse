# 🚀 ПОДГОТОВКА ПРОЕКТА К РАЗВЕРТЫВАНИЮ НА VERCEL

## ⚡ БЫСТРЫЙ СТАРТ (3 способа):

---

### 🖱️ СПОСОБ 1: АВТОМАТИЧЕСКИЙ (Рекомендуется)

#### Для Windows:
```bash
# Просто дважды кликните на файл:
prepare-for-vercel.bat
```

#### Для Mac/Linux:
```bash
# Сделайте файл исполняемым и запустите:
chmod +x prepare-for-vercel.sh
./prepare-for-vercel.sh
```

**✅ Скрипт автоматически:**
- Сохранит старый `index.html` → `index-old-standalone.html`
- Переименует `index-vite.html` → `index.html`
- Подготовит проект к деплою

---

### 👐 СПОСОБ 2: РУЧНОЙ

1. Переименуйте файлы вручную:
   ```
   index.html          →  index-old-standalone.html
   index-vite.html     →  index.html
   ```

2. Готово!

---

### 💻 СПОСОБ 3: ЧЕРЕЗ КОМАНДНУЮ СТРОКУ

#### Windows (CMD):
```cmd
rename index.html index-old-standalone.html
rename index-vite.html index.html
```

#### Mac/Linux (Terminal):
```bash
mv index.html index-old-standalone.html
mv index-vite.html index.html
```

---

## 📋 ПОСЛЕ ПОДГОТОВКИ:

### 1. Загрузите на GitHub:
```bash
git init
git add .
git commit -m "Initial commit - Bento Cakes Landing ready for Vercel"
git branch -M main
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. Разверните на Vercel:
1. Откройте https://vercel.com
2. Нажмите **"New Project"**
3. Выберите **"Import Git Repository"**
4. Выберите ваш репозиторий
5. Нажмите **"Deploy"**

**Настройки определятся автоматически:**
- Framework: Vite ✅
- Build Command: `npm run build` ✅
- Output Directory: `dist` ✅

---

## ✅ ЧТО УЖЕ НАСТРОЕНО:

| Файл | Статус | Что делает |
|------|--------|------------|
| `vercel.json` | ✅ | Конфигурация Vercel (роутинг, кэширование, security) |
| `vite.config.ts` | ✅ | Настройки сборки Vite |
| `index-vite.html` | ✅ | Правильная точка входа для React |
| `.vercelignore` | ✅ | Исключает ненужные файлы при деплое |
| `package.json` | ✅ | Все зависимости готовы |

---

## 🔍 ПРОВЕРКА ПЕРЕД ДЕПЛОЕМ:

Протестируйте проект локально:
```bash
npm install
npm run build
npm run preview
```

Если всё работает локально → будет работать на Vercel!

---

## 📱 СТРУКТУРА ПОСЛЕ ПОДГОТОВКИ:

```
/
├── index.html                    ← ✅ Новый (для Vite/Vercel)
├── index-old-standalone.html     ← 💾 Старый (сохранен)
├── vercel.json                   ← ⚙️ Конфигурация Vercel
├── vite.config.ts                ← ⚙️ Настройки Vite
├── package.json                  ← 📦 Зависимости
├── src/
│   ├── main.tsx                  ← 🚀 Точка входа React
│   ├── App.tsx                   ← 📄 Главный компонент
│   └── ...
└── dist/                         ← 📦 Создается при сборке
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ:

### Q: Зачем переименовывать index.html?
**A:** Старый `index.html` - это standalone-версия со всем HTML внутри. Новый `index-vite.html` - это точка входа для React-приложения, которое собирается через Vite.

### Q: Что если я сделал ошибку?
**A:** Старый файл сохранен как `index-old-standalone.html` - можете вернуть его обратно в любой момент.

### Q: Можно ли использовать оба файла?
**A:** Да! Старый `index-old-standalone.html` можно задеплоить отдельно на любой хостинг как обычную HTML-страницу.

### Q: Что делать если ошибка при деплое?
**A:** 
1. Проверьте что переименовали файл
2. Убедитесь что `index.html` содержит: `<script type="module" src="/src/main.tsx"></script>`
3. Проверьте логи в Vercel Dashboard

---

## 🎯 ФИНАЛЬНЫЙ ЧЕК-ЛИСТ:

- [ ] Запустил скрипт `prepare-for-vercel.bat` (Windows) или `prepare-for-vercel.sh` (Mac/Linux)
- [ ] Проверил что `index.html` правильный (содержит `<div id="root"></div>`)
- [ ] Протестировал локально: `npm run build && npm run preview`
- [ ] Загрузил на GitHub
- [ ] Создал проект в Vercel
- [ ] Импортировал репозиторий
- [ ] Нажал Deploy
- [ ] Проверил что сайт работает
- [ ] Протестировал все страницы и функции
- [ ] Проверил Instagram-ссылки
- [ ] Протестировал на мобильных устройствах

---

## 🎉 ГОТОВО!

После выполнения всех шагов ваш сайт будет доступен по адресу:
```
https://your-project.vercel.app
```

**Можете подключить свой домен в настройках Vercel!**

---

## 📞 ПОДДЕРЖКА:

Если что-то пошло не так:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь что правильный `index.html` (с `<div id="root"></div>`)
3. Проверьте что все файлы загружены на GitHub
4. Попробуйте пересобрать: `npm run build`

---

🚀 **УДАЧНОГО ДЕПЛОЯ!** 🎊
