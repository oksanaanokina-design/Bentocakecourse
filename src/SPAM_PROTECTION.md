# 🛡️ Защита от спама

## Обзор системы

Многоуровневая система защиты от спама и злоупотреблений, состоящая из нескольких слоёв:

1. ✅ **Rate Limiting** - ограничение частоты запросов
2. ✅ **Honeypot** - ловушки для ботов
3. ✅ **Throttle/Debounce** - защита от случайных множественных кликов
4. ✅ **Browser Fingerprint** - идентификация пользователей
5. ✅ **Progressive Penalties** - прогрессивные штрафы при злоупотреблениях
6. ✅ **Activity Monitoring** - отслеживание подозрительной активности

---

## 1️⃣ Rate Limiting (Ограничение частоты)

### Что это?
Система, которая ограничивает количество действий в определённый период времени.

### Настройки

```typescript
SPAM_PROTECTION = {
  // Платежи - очень строгая защита
  PAYMENT: {
    maxAttempts: 3,        // Максимум 3 попытки
    windowMs: 60000,       // За 60 секунд (1 минута)
    progressivePenalty: true, // Штрафы увеличиваются
  },
  
  // Кнопки - средняя защита
  BUTTON: {
    maxAttempts: 5,        // Максимум 5 кликов
    windowMs: 10000,       // За 10 секунд
    progressivePenalty: false,
  },
  
  // Навигация - мягкая защита
  NAVIGATION: {
    maxAttempts: 10,       // Максимум 10 переходов
    windowMs: 5000,        // За 5 секунд
    progressivePenalty: false,
  },
  
  // Модальные окна
  MODAL: {
    maxAttempts: 8,        // Максимум 8 открытий
    windowMs: 30000,       // За 30 секунд
    progressivePenalty: true,
  },
}
```

### Использование

```typescript
import { isActionAllowed } from './utils/spamProtection';

function handlePayment() {
  const check = isActionAllowed('PAYMENT', 'unique_action_id');
  
  if (!check.allowed) {
    console.warn('Rate limit exceeded:', check.reason);
    if (check.cooldown) {
      alert(`Подождите ${check.cooldown} секунд`);
    }
    return;
  }
  
  // Выполняем действие
  processPayment();
}
```

---

## 2️⃣ Honeypot Protection (Ловушка для ботов)

### Что это?
Невидимое поле в форме. Человек его не видит и не заполняет, но бот автоматически заполнит.

### Как работает?

```tsx
import HoneypotField from './components/HoneypotField';

<form>
  {/* Обычные поля */}
  <input type="email" name="email" />
  
  {/* Honeypot - невидимое поле */}
  <HoneypotField onBotDetected={() => {
    console.warn('Bot detected!');
    // Блокируем отправку формы
  }} />
  
  <button type="submit">Отправить</button>
</form>
```

### Проверки Honeypot

1. **Заполнение скрытого поля** - если заполнено = бот
2. **Скорость заполнения** - если форма заполнена < 2 секунд = подозрительно
3. **Множественные поля** - несколько honeypot полей для надёжности

---

## 3️⃣ Throttle и Debounce

### Throttle
Ограничивает частоту вызовов функции. Максимум 1 вызов в N миллисекунд.

```typescript
import { throttle } from './utils/spamProtection';

// Функция может вызываться максимум раз в 2 секунды
const handleClick = throttle(() => {
  console.log('Click!');
}, 2000);
```

**Использование:**
- Кнопки оплаты
- Открытие модальных окон
- Отправка форм

### Debounce
Откладывает выполнение до окончания серии вызовов.

```typescript
import { debounce } from './utils/spamProtection';

// Выполнится только после 500мс тишины
const handleSearch = debounce((query) => {
  searchAPI(query);
}, 500);
```

**Использование:**
- Поиск при вводе
- Автосохранение
- Валидация форм

---

## 4️⃣ Browser Fingerprint

### Что это?
Уникальный идентификатор браузера для отслеживания злоупотреблений.

### Что собирается?

- User Agent
- Язык браузера
- Разрешение экрана
- Глубина цвета
- Временная зона
- Canvas fingerprint

### Использование

```typescript
import { BrowserFingerprint } from './utils/spamProtection';

const fingerprint = BrowserFingerprint.getFingerprint();
console.log('Fingerprint:', fingerprint);
// Например: "a7f3e2d9c4"
```

---

## 5️⃣ Progressive Penalties (Прогрессивные штрафы)

### Как работает?

При включении `progressivePenalty: true`:

1. **1-я попытка превышения** → блокировка на 1x время окна
2. **2-я попытка превышения** → блокировка на 2x время окна
3. **3-я попытка превышения** → блокировка на 3x время окна
4. И так далее...

### Пример

```
Лимит: 3 попытки за 60 секунд

Попытка 1, 2, 3 → OK ✅
Попытка 4 → БЛОК на 60 секунд ⏱️
Попытка 5 (во время блока) → БЛОК на 120 секунд ⏱️⏱️
Попытка 6 (во время блока) → БЛОК на 180 секунд ⏱️⏱️⏱️
```

---

## 6️⃣ Activity Monitoring (Мониторинг активности)

### Что отслеживается?

- Общее количество действий за сессию
- Подозрительные паттерны
- Автоматическая блокировка при > 100 действий

### Черный список

```typescript
import { activityMonitor } from './utils/spamProtection';

// Проверка
if (activityMonitor.isBlacklisted(fingerprint)) {
  console.log('User is blacklisted!');
  return;
}
```

---

## 📊 Архитектура защиты

```
Пользователь пытается выполнить действие
           ↓
┌──────────────────────────────┐
│ 1. Проверка черного списка   │
│    activityMonitor           │
└──────────────────────────────┘
           ↓ (passed)
┌──────────────────────────────┐
│ 2. Rate Limiting             │
│    advancedRateLimiter       │
└──────────────────────────────┘
           ↓ (passed)
┌──────────────────────────────┐
│ 3. Honeypot Check            │
│    HoneypotProtection        │
└──────────────────────────────┘
           ↓ (passed)
┌──────────────────────────────┐
│ 4. Отслеживание активности   │
│    activityMonitor.track()   │
└──────────────────────────────┘
           ↓
     ✅ Действие разрешено
```

---

## 🔧 Примеры использования

### PaymentModal (Модальное окно оплаты)

```typescript
import { isActionAllowed, HoneypotProtection } from './utils/spamProtection';
import HoneypotField from './components/HoneypotField';

function PaymentModal() {
  const [isBotDetected, setIsBotDetected] = useState(false);
  
  const handlePayment = () => {
    // 1. Проверка honeypot
    if (isBotDetected) {
      console.warn('Bot detected!');
      return;
    }
    
    // 2. Rate limiting
    const check = isActionAllowed('PAYMENT', 'payment_action');
    if (!check.allowed) {
      alert(`Подождите ${check.cooldown} секунд`);
      return;
    }
    
    // 3. Всё ок - продолжаем
    window.open(paymentUrl);
  };
  
  return (
    <form>
      <HoneypotField onBotDetected={() => setIsBotDetected(true)} />
      <button onClick={handlePayment}>Оплатить</button>
    </form>
  );
}
```

### Защищённая кнопка

```typescript
import { throttle } from './utils/spamProtection';

const handleClick = throttle(() => {
  console.log('Action performed!');
}, 2000); // Максимум раз в 2 секунды
```

### Форма с защитой

```typescript
import { debounce, isActionAllowed } from './utils/spamProtection';

function ContactForm() {
  const handleSubmit = (e) => {
    e.preventDefault();
    
    const check = isActionAllowed('BUTTON', 'contact_form');
    if (!check.allowed) {
      alert('Слишком много попыток. Подождите немного.');
      return;
    }
    
    submitForm();
  };
  
  // Debounce для валидации при вводе
  const validateEmail = debounce((email) => {
    // Валидация
  }, 500);
  
  return (
    <form onSubmit={handleSubmit}>
      <input onChange={(e) => validateEmail(e.target.value)} />
      <HoneypotField />
      <button type="submit">Отправить</button>
    </form>
  );
}
```

---

## 🎯 Рекомендации

### Для платежей
```typescript
✅ PAYMENT config (3 попытки / 60 сек)
✅ Honeypot field
✅ Throttle на кнопке (1000ms)
✅ Progressive penalties
```

### Для кнопок
```typescript
✅ BUTTON config (5 попыток / 10 сек)
✅ Throttle (500-1000ms)
```

### Для форм
```typescript
✅ BUTTON config
✅ Honeypot field
✅ Debounce для валидации (300-500ms)
```

### Для навигации
```typescript
✅ NAVIGATION config (10 попыток / 5 сек)
✅ Throttle (200ms)
```

---

## 🚨 Мониторинг

### Console warnings

Все попытки злоупотреблений логируются в консоль:

```
[Security] Rate limit exceeded: Rate limit exceeded
[Security] Bot detected via honeypot
[Security] Suspicious activity detected: a7f3e2d9c4
```

### Production

В продакшене можно отправлять события на сервер:

```typescript
if (!check.allowed) {
  // Отправить на сервер для анализа
  analytics.track('spam_attempt', {
    fingerprint,
    action: 'PAYMENT',
    reason: check.reason,
  });
}
```

---

## 📈 Статистика

Текущая защита предотвращает:

- ✅ Случайные двойные клики (throttle)
- ✅ Спам-боты (honeypot)
- ✅ DDoS атаки на стороне клиента (rate limiting)
- ✅ Злонамеренные пользователи (progressive penalties)
- ✅ Автоматические скрипты (browser fingerprint)

---

## 🔒 Безопасность

**Важно:** Это защита на стороне клиента. Она не заменяет серверную защиту!

### Клиентская защита (то, что есть):
- Улучшает UX
- Предотвращает случайные ошибки
- Отпугивает простых ботов
- Снижает нагрузку на сервер

### Серверная защита (нужна дополнительно):
- Валидация данных
- Rate limiting на API
- CAPTCHA при подозрительной активности
- IP блокировка

---

*Документация по защите от спама v1.0 | Январь 2026*
